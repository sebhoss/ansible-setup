---
- name: create mpv config directory
  file:
    path: "{{ user.home }}/{{ mpv.config.home }}"
    mode: 0700
    state: directory
  tags: mpv

- name: copy mpv config
  copy:
    src: config
    dest: "{{ user.home }}/{{ mpv.config.home }}/config"
  tags: mpv
