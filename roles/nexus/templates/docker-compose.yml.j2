# {{ ansible_managed }}

version: '2'

services:
  nexus:
    image: sonatype/nexus:{{ nexus.version }}
    container_name: local-nexus
    volumes:
      - ./data:/sonatype-work
    ports:
     - {{ ports.nexus.http }}:8081
    expose:
     - 8081
    network_mode: bridge
